import { FilterByEventTitlePipe } from './filter-by-event-title.pipe';

describe('FilterByEventTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEventTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
