import { FilterCompanyEmployeePipe } from './filter-company-employee.pipe';

describe('FilterCompanyEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompanyEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});
