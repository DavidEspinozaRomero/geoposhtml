import { FilterByEventDatePipe } from './filter-by-event-date.pipe';

describe('FilterByEventDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEventDatePipe();
    expect(pipe).toBeTruthy();
  });
});
