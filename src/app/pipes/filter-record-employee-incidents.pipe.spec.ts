import { FilterRecordEmployeeIncidentsPipe } from './filter-record-employee-incidents.pipe';

describe('FilterRecordEmployeeIncidentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecordEmployeeIncidentsPipe();
    expect(pipe).toBeTruthy();
  });
});
